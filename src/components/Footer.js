import React from "react"

export default function(){
    return(
        <footer>
            <section className="footer-container">
                <span>Open Job Listing © {new Date().getFullYear()}</span>
            </section>
        </footer>
    )
}